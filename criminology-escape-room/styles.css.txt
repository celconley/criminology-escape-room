/* General Body Styles */
body {
    font-family: 'Arial', sans-serif;
    background-color: #1a1a1a;
    color: #fff;
    margin: 0;
    padding: 0;
    text-align: center;
    overflow-x: hidden;
}

header {
    background-color: #333;
    padding: 20px 0;
    border-bottom: 2px solid #fff;
}

header h1 {
    font-size: 2.5em;
    margin: 0;
}

header p {
    font-size: 1.2em;
    margin-top: 10px;
}

footer {
    background-color: #333;
    color: #fff;
    padding: 10px 0;
    position: fixed;
    bottom: 0;
    width: 100%;
}

/* Hidden Class for Station Visibility */
.hidden {
    display: none;
}

/* Station Styles */
.station {
    margin: 20px auto;
    padding: 20px;
    max-width: 800px;
    background-color: #444;
    border-radius: 10px;
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
}

/* Buttons */
button {
    background-color: #ff5c5c;
    border: none;
    color: white;
    padding: 10px 20px;
    font-size: 1.1em;
    cursor: pointer;
    border-radius: 5px;
    transition: background-color 0.3s ease;
}

button:hover {
    background-color: #ff3333;
}

/* Form Styles */
form {
    margin-top: 20px;
}

input[type="text"], textarea {
    width: 80%;
    padding: 10px;
    margin-top: 10px;
    border: none;
    border-radius: 5px;
    background-color: #666;
    color: #fff;
    font-size: 1em;
}

input[type="text"]:focus, textarea:focus {
    outline: none;
    background-color: #555;
}

/* Suspect Profile Styles */
.suspect-profile {
    margin: 15px;
    padding: 15px;
    background-color: #333;
    border-radius: 10px;
    display: inline-block;
    width: 200px;
    text-align: center;
}

.suspect-profile img {
    width: 100%;
    border-radius: 8px;
}

.suspect-profile h3 {
    font-size: 1.2em;
    margin: 10px 0;
}

/* Puzzle Image Styles */
.evidence-puzzle, .crime-scene-image {
    max-width: 100%;
    border-radius: 10px;
    margin-top: 20px;
}

/* Textarea Styles */
textarea {
    width: 80%;
    height: 100px;
    margin-top: 10px;
    padding: 10px;
    background-color: #666;
    border: none;
    border-radius: 5px;
    color: #fff;
    font-size: 1em;
}

textarea:focus {
    outline: none;
    background-color: #555;
}

/* Background Animation for Coding Effect */
body {
    position: relative;
    overflow: hidden;
}

body::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: url('images/coding-background.gif') repeat;
    opacity: 0.05;
    z-index: -1;
}
